# OMT Config

Framework defaults live here.

- `oh-my-trellis.defaults.jsonc` defines built-in defaults.
- `oh-my-trellis.jsonc` at the project root is the override layer.

The root override file is the only intended user-edit entrypoint for model routing and workflow options.
