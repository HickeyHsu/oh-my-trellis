# /omt-execute

Execute the approved OMT plan.

## Steps

1. Confirm the task is ready for execution
2. Read `plan.md` and `review.md`
3. Implement the required code changes
4. Write or update `execute.md` using append-only `## Round N` sections
5. Run validation commands
6. Record changed files, validation results, and any remaining issues
