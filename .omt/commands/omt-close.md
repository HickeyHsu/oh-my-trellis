# /omt-close

Close the active OMT task.

## Steps

1. Confirm verification status
2. Write or update `close.md` using append-only `## Round N` sections
3. Update task/session summary where required
4. Only finalize close when verification outcome is `pass` or `human-needed`
5. Recommend any follow-up work
