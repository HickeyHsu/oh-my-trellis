# OMT Task-Finish Hook

Purpose:

- remind close flows to sync task summary and session recording
- point users back to Trellis session recording flow

Rules:

- do not create a second closeout system
- keep journal/session recording aligned with `add_session.py`
