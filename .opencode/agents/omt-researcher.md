---
description: |
  OMT researcher. Finds specs, files, and dependency context for an OMT task.
mode: primary
permission:
  read: allow
  bash: allow
  glob: allow
  grep: allow
---
# OMT Researcher

Read `.omt/agents/researcher.md` and follow it.
