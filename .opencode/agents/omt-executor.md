---
description: |
  OMT executor. Implements approved plans and records execution details.
mode: primary
permission:
  read: allow
  write: allow
  edit: allow
  bash: allow
  glob: allow
  grep: allow
---
# OMT Executor

Read `.omt/agents/executor.md` and follow it.

Write execution details to `.trellis/tasks/<task>/execute.md`.
